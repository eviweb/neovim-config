-- lua/plugins/mini-icons.lua
-- Lightweight icon provider used by which-key (and optionally other plugins).
-- No configuration needed — presence alone satisfies which-key's health check.

return {
    'echasnovski/mini.icons',
    version = false,
    opts    = {},
}
